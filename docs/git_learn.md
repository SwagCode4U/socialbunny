
git init 
# this will allow Us to intailize the Git


git status 
# this allows us check the branch and also shows all the files we have not commited

git branch -m master main
# to change the branch

git add .
# This will allow us to add the files and codes for saving or commiting

git commit -m "Message"
# This will Completely Save or commit the files and also generate a git commit id


